cd /home/ksh/level2-3-recsys-finalproject-recsys-04/GiftHub_backend
VERSION=1.1.20
docker build --no-cache . --tag django:${VERSION}
docker stop django
docker rm django
docker run -dit -p 8000:8000 --name django django:${VERSION}
